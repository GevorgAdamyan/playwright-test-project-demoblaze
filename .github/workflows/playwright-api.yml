name: Playwright Tests - API Only

on:
  workflow_dispatch: # Allow manual trigger only

jobs:
  api-test:
    name: Run API Tests
    timeout-minutes: 30
    runs-on: ubuntu-latest
    
    env:
      # Environment variables for the tests
      BASE_URL: https://demoblaze.com
      BASE_URL_API: https://api.demoblaze.com
      CI: true
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium
      
      - name: Create environment file
        run: |
          echo "BASE_URL=${{ env.BASE_URL }}" >> .env
          echo "BASE_URL_API=${{ env.BASE_URL_API }}" >> .env
          echo "USERNAME=${{ secrets.USERNAME }}" >> .env
          echo "PASSWORD=${{ secrets.PASSWORD }}" >> .env
      
      - name: Create auth directory
        run: mkdir -p .auth
      
      - name: Run API tests
        run: npx playwright test tests/API --reporter=html,github
        
      - name: Upload API test results
        uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: api-test-report-${{ github.run_number }}
          path: playwright-report/
          retention-days: 30
      
      - name: Upload test artifacts
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: api-test-results-${{ github.run_number }}
          path: test-results/
          retention-days: 7
      
      - name: Publish API test results summary
        if: always()
        run: |
          echo "## 🔌 API Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ -f "playwright-report/index.html" ]; then
            echo "✅ API test report generated successfully" >> $GITHUB_STEP_SUMMARY
            echo "📊 View the detailed report in the artifacts section" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ API test report generation failed" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Test Suite:** API Tests Only" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "**Branch:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY